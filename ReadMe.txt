Kreirati bazu sa nazivom wt24
username i password na bazu su root, password

Pozovite priprema.js (u folderu config) kako bi se kreirali pocetni podaci (node config/priprema.js)

Pozovite index.js za pokretanje aplikacije (node index.js)